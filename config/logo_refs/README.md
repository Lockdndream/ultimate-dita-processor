Place approved logo reference images in this folder for PDF quality checks.

Suggested structure:

- `config/logo_refs/invenco/*.png`
- `config/logo_refs/gilbarco/*.png`
- `config/logo_refs/gasboy/*.png`
- `config/logo_refs/angi/*.png`

Use cropped logo/header snippets from approved PDFs when possible.
PNG with transparent background is preferred.
